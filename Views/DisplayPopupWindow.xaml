<Window x:Class="BorderlessWindowApp.Views.DisplayPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="Manual"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Grid Background="#CC000000">
        <TextBlock x:Name="Label" Foreground="White" FontSize="48" HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontWeight="Bold" FontFamily="Segoe UI Variable Display, Segoe UI" />
    </Grid>
</Window>